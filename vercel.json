{
	"$schema": "https://openapi.vercel.sh/vercel.json",
	"buildCommand": "pnpm generate-favicon && pnpm build",
	"installCommand": "pnpm install --frozen-lockfile",
	"framework": "nextjs",
	"outputDirectory": ".next"
}
